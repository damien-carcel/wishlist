version: 3

tasks:
  up:
    aliases:
      - u
    cmds:
      - docker compose up --detach --wait database
    desc: Start the database container

  test:create:
    aliases:
      - t:c
      - test:c
      - t:create
    cmds:
      - docker compose run --env APP_ENV=test --rm --user "{{.HOST_USER_IDS}}" php bin/console doctrine:database:create --if-not-exists
      - task: migrate
        vars:
          APP_ENV: test
    desc: Create the test database (the "regular" database is created automatically at container startup)

  migrate:
    aliases:
      - m
    cmds:
      - docker compose run --env APP_ENV="{{.APP_ENV}}" --rm --user "{{.HOST_USER_IDS}}" php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration
    desc: Run database migrations
    vars:
      APP_ENV: '{{.APP_ENV| default "dev"}}'
