version: 3

tasks:
  bash:
    aliases:
      - b
    cmds:
      - docker compose run --rm --remove-orphans app bash
    desc: Launch a shell in the app container

  down:
    aliases:
      - d
    cmds:
      - docker compose down -v --remove-orphans
    desc: Stop and remove all containers
