version: '3'

# TODO: Install should require setup
# TODO: All run and tests steps should check for the dependencies to be installed

silent: true

includes:
  setup:
    taskfile: ./taskfiles/setup.yaml
    aliases: [s]
  docker:
    taskfile: ./taskfiles/docker.yaml
    aliases: [dc]
  dependencies:
    taskfile: ./taskfiles/dependencies.yaml
    aliases: [d]
  run:
    taskfile: ./taskfiles/run.yaml
    aliases: [r]
  tests:
    taskfile: ./taskfiles/tests.yaml
    aliases: [t]

tasks:
  default:
    cmds:
      - task --list
